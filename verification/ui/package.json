{
  "name": "hellotime-ui-smoke",
  "private": true,
  "description": "UI 冒烟测试：Playwright 驱动 React 前端跑主流程",
  "scripts": {
    "test": "playwright test",
    "install-browsers": "playwright install chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.0"
  }
}
