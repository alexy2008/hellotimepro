#!/usr/bin/env bash
# 类型检查（tsc --noEmit）
set -euo pipefail

HERE="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd)"
cd "$HERE"

if [[ ! -d node_modules ]]; then
  npm install --silent
fi

exec npm test -- "$@"
