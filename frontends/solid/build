#!/usr/bin/env bash
# 生产构建：tsc 类型检查 + vite build，产物输出到 ./dist
set -euo pipefail

HERE="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd)"
cd "$HERE"

if [[ ! -d node_modules ]]; then
  npm install --silent
fi

npm run build
echo "✓ 构建完成：$(pwd)/dist"
