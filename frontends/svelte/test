#!/usr/bin/env bash
# 跑单元测试（vitest）
set -euo pipefail

HERE="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd)"
cd "$HERE"

if [[ ! -d node_modules ]]; then
  echo "首次运行：npm install"
  npm install --silent
fi

exec npm test
